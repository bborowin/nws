"""
  Logic for parsing raw story data into structured Stories
"""

from baxter.dl.models import Source, Story, StoryElement, ExtractCmd, SoupCmd, ParserCmd

class Parser:
  
  # load a story 
  def load(url):
    pass
